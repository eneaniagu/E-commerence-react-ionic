import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'virtuegadget',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
